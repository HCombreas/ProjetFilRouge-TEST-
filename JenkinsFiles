pipeline {
    agent any
    stages {
        stage('SCM') {
            steps {
                checkout scm
            }
        }
        stage('SonarQube Analysis') {
            environment{
                scannerHome = tool 'SonarqubeScanner'
                SONAR_API_TOKEN=credentials('test')
            }
            steps {
                script {
            
                    sh "$scannerHome/bin/sonar-scanner"
                }
            }
        }
    }
}