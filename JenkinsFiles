pipeline {
    agent any
    stages {
        stage('SCM') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                sh 'su -c "apt install npm"'
                sh 'export PATH=$PATH:/usr/bin/npm'
            }
        }
        stage('SonarQube Analysis') {
            steps {
                script {
                    def scannerHome = tool 'SonarqubeScanner'
                    sh "${scannerHome}/bin/sonar-scanner"
                }
            }
        }
    }
}
